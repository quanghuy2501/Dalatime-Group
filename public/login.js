const form=document.querySelector('#loginForm'), error=document.querySelector('#error'), button=document.querySelector('.submit'), password=document.querySelector('#password'), toggle=document.querySelector('#toggle');
toggle.addEventListener('click',()=>{const showing=password.type==='text';password.type=showing?'password':'text';toggle.textContent=showing?'Hiện':'Ẩn';toggle.setAttribute('aria-label',showing?'Hiện mật khẩu':'Ẩn mật khẩu')});
form.addEventListener('submit',async event=>{event.preventDefault();error.textContent='';button.disabled=true;button.classList.add('loading');try{const response=await fetch('/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:form.username.value,password:form.password.value})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Không thể đăng nhập lúc này');window.location.href=data.redirect||'/'}catch(err){error.textContent=err.message;button.disabled=false;button.classList.remove('loading')}});
